1.重写和重载的区别  面向对象4个特征的哪一个的体现
    @1、重写
        > 重写体现多态性，子类在继承父类后可以重写父类中的某些方法，那么就可以在编译期调用父类中的方法,
        但是在运行期，实际调用的可以是子类重写的方法
    @2、重载
        > 重载指的是在同一个类中，允许存在一个以上的同名方法，只要他们的形参列表不同即可

2.final finally finalize()区别
    > final
        用于声明属性，方法和类，分别表示属性不可交变，方法不可重写，类不可继承
    > finally
        异常处理语句结构的一部分，finally中的语句一定会执行
    > finalize()
        是Object类的一个方法，在垃圾收集器执行的时候会调用被回收对象的此方法，供垃圾收集时的其他资源回收，例如关闭文件等

3.java4个访问修饰符的特定
    > public 同一个工程下可以访问
    > protected 指同一个package或者不同的package中的子类都能访问
    > 缺省 同一个包中可以访问
    > private 本类中可以访问

4.为什么方法的重载不能根据返回值类型的区分
    > 对象在调用方法时，必须要根据方法名和形参列表来判断调用的是哪个方法，如果仅凭返回值无法确定
    调用的是哪个方法

5.java对象创建方式
    > 使用new关键字
    > 使用反射创建对象 Class类的newInstance方法
    > 使用clone方法
    > 使用序列化与反序列化 Serializable接口

6.static关键字的使用
    > static可以修饰属性、方法、代码块、内部类
    > 随着类的加载而加载，只加载一次

7.java的基本数据类型和包装类
    > byte   1字节    Byte
    > short  2字节    Short
    > int    4字节    Integer
    > long   8字节    Long
    > float  4字节    Float   单精度
    > double 8字节    Double  双精度
    > char   2字节    Character
    > boolean 布尔型

8.动态绑定的前提条件(多态性使用的前提条件)
    > 类的继承关系
    > 方法的重写
    > 体现是：编译期调用父类中声明的方法 运行期执行的是子类重写的方法

9.&& 和 & 区别
    > && 条件中只要有一个为false 后面的都不进行判断
    > &  所有条件都进行判断

10.位运算符
    > 左移 相当于在原来的基础上乘2 格式：i<<2 i=ix2x2
    > 右移 相当于在原来的基础上除2 格式：i>>2 i=i/2/2